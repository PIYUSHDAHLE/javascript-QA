// Question: Write a function that checks if a number is even or odd using the ternary operator.

function op(a) {
       return (a%2==0 ? `${a} is even` : `${a} is odd` )    
}

console.log(op(77));
